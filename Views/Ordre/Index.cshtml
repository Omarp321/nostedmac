@model IEnumerable<Nøsted.Models.Ordre>

@{
    ViewData["Title"] = "Index";
}

<h1>Alle ordre</h1>

<p>
    <a asp-action="Create">Lag ny ordre</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OrdreStatus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrdreInformasjon)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OrdreStatus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrdreInformasjon)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OrdreId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OrdreId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OrdreId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
